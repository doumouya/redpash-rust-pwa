---
title: WASM engine
section: Internal
order: 28
last modified date: 2026-05-24
owner: Gus
status: stub
---

# WASM engine

> **TODO (Gus).** Fill from `data::wasm`, `tools/build-wasm.sh`, `frontend/scripts/wasm-engine.js`, `frontend/wasm/README.md`.

To cover:

- The four wasm-bindgen entry points: `apply_filter`, `apply_sort`, `auto_clean`, `step_preview`
- Build pipeline: `cargo build --release --target wasm32-unknown-unknown` → wasm-bindgen → wasm-opt → `frontend/wasm/`
- Why the .wasm is gitignored (regenerated by `sh tools/build-wasm.sh`)
- The 5 MB capacity gate (`gateBySize` in wasm-engine.js) — capacity vs capability
- `console_error_panic_hook` — Rust panics → browser console
- Serial dedup on wasm32 (no rayon POOL) — see commit 42fb49d
- See also: [architecture/roadmap-webassembly.md](../architecture/roadmap-webassembly.md) for the phasing
