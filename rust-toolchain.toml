# Stable only — polars (arriving Phase 3) breaks on nightly-internal unicode
# imports, and nothing here needs nightly features. wasm32 target is required
# from day one: tools/purity-check.sh gates the data crate on it.
# Reasoning inherited from redpash-rust-pwa rust-toolchain.toml (2026-06-05).
[toolchain]
channel = "stable"
targets = ["wasm32-unknown-unknown"]
