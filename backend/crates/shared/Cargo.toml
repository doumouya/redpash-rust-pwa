[package]
name = "shared"
version = "0.1.0"
edition = "2021"

# Wire DTOs only. No logic, no io — both api (native) and data (wasm32)
# depend on this crate, so it must stay as light as the boundary it defines.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
