// Reports page — Phase 3 MVP. Delegates to /scripts/reports/index.js.

import { mount as mountReports } from "/scripts/reports/index.js";

export default function mount(root) {
  mountReports(root);
}
