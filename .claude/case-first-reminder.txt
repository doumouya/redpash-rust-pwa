[Case-first default] If this turn will implement a NON-TRIVIAL change — a feature, a logic bugfix, a multi-file edit, anything that will produce a commit — then FIRST open a Case (mcp__redpash-slack__case_create: title + a one-paragraph plan as the description), then log plan → key decisions → outcome via case_comment and move status (in_progress → in_review → done) — before `done`, reconcile the docs (REDMAP + INDEX + the per-area doc) in a commit referencing the Case, or declare `Docs: n/a — <reason>` (the close is gated on it). Commit on the branch; NEVER push without Em (the one hard gate — the Case thread is his async review surface). SKIP all of this for: trivial one-line / doc / typo fixes, read-only investigation, and pure Q&A / conversation. Reserve the full /feature role-chain (architect→tester→coder→reviewer→ops) for BIG or RISKY features. If the Cases MCP backend is down, file an on-disk Case stub under docs/internal/cases/ and promote it via case_create when the backend is back.
